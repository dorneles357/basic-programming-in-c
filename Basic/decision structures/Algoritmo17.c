/*
Faça um algoritmo que determine o maior numero entre N números 
A condição de parada é a entrada de um valor o ou seja, o algoritmo
deve ficar calculando o maior qté que a entrada seja igual a 0.
*/

#include<stdio.h>

int main(){
    //Declaração de variável 
    int n, maior = 0;

    //Entrada de dados 
    printf("informe o número:");
    scanf("%d", &n);

    //Processamento e saída de dados 
        while (n != 0)
        {
            if (n > maior)
            {
                maior =;
            }
    //Entrada de dados 
            printf("informe o número:");
            scanf("%d", &n);
        }
         printf(" O maior número é %d", maior);
        
}